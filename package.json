{
  "name": "tu-proyecto",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^11.8.1",
    "three": "^0.162.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^6.3.5"
  }
}
