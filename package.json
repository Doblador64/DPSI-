{
  "name": "tu-proyecto",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.162.0",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}